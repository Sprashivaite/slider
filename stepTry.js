
// let arr = []

// for(let i = 0;i <= field.offsetWidth; i += stepPX) {
//   arr.push(i)
// }
// console.log(arr);

// let step = 10;

// let count = 1;
// let prevVal = min;


// let stepPX = step * (field.offsetWidth - button.offsetWidth / 2) / (max - min);
// stepPX = Math.trunc(stepPX);
// console.log(stepPX);


// if( (shiftLeft) - prevVal > stepPX/2) {button.style.left = stepPX  + prevVal + 'px'; prevVal = button.offsetLeft}
          
          // else if( (shiftLeft) < prevVal - stepPX/2) {button.style.left = prevVal - stepPX + 'px'; prevVal = button.offsetLeft}

          // for (const item of arr) {
          //   if ( shiftLeft == item) {button.style.left = item -  + 'px'}
          // }

          // if ((shiftLeft) % stepPX  == 0) { button.style.left = shiftLeft + 'px'; }