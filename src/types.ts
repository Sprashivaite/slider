type ViewData = {
    button: HTMLElement;
    buttonOffset: number;
    mouseCoords: number;
    value?: number;
}
type scaleValues = {
    arrValues: number[];
    quantity: number;
}
export {ViewData, scaleValues }